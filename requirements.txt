# ============================================================
# requirements.txt — Python dependencies
#
# Install all with:  pip install -r requirements.txt
#
# Organised by category so it's easy to understand what each
# library is for.
# ============================================================

# ---- Web Framework ----
fastapi>=0.115.0          # The API framework — modern, fast, auto-generates API docs
uvicorn[standard]>=0.32.0 # ASGI server that runs FastAPI
python-multipart>=0.0.12  # Required for file uploads in FastAPI

# ---- Authentication ----
python-jose[cryptography]>=3.3.0  # JWT token creation and validation
passlib[bcrypt]>=1.7.4            # Password hashing (bcrypt is the gold standard)
bcrypt>=4.1.0

# ---- Database ----
asyncpg>=0.30.0           # Async PostgreSQL driver (fastest for Python async)
sqlalchemy>=2.0.0         # ORM / query builder — gives us Python classes for DB tables
psycopg2-binary>=2.9.9    # Sync PostgreSQL driver (used in scripts and migrations)
alembic>=1.14.0           # Database migrations (like Git for your database schema)

# ---- Gemini AI ----
google-genai>=1.0.0          # Gemini API client (embeddings + text generation) — new SDK

# ---- Document Parsing ----
python-docx>=1.1.0        # Read .docx Word documents
PyMuPDF>=1.25.0           # Read .pdf files (also known as fitz) — fast + renders pages to images for OCR
pypdf>=5.0.0              # Alternative PDF parser (more robust for some PDFs)
pywin32>=306; sys_platform == "win32"  # Read old .doc files (Word 97-2003) via MS Word COM automation

# ---- Data Processing ----
pandas>=2.2.0             # DataFrames — perfect for the evaluation spreadsheet
numpy>=2.0.0              # Numerical operations (cosine similarity calculations)
scikit-learn>=1.5.0       # Cosine similarity, metrics like NDCG

# ---- Output / Reports ----
openpyxl>=3.1.0           # Read/write .xlsx Excel files (evaluation spreadsheet)

# ---- Utilities ----
python-dotenv>=1.0.0      # Load .env files into environment variables
pydantic>=2.10.0          # Data validation (FastAPI uses this internally)
pydantic-settings>=2.7.0  # Load settings from .env into Pydantic models
httpx>=0.28.0             # HTTP client (used in tests and API calls)
tenacity>=9.0.0           # Retry logic with backoff (for flaky API calls)
tqdm>=4.67.0              # Progress bars for long-running scripts

# ---- Logging / Monitoring ----
loguru>=0.7.0             # Better logging than Python's built-in logging module
